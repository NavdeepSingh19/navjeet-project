# In dev, requests to /api are proxied to the backend by vite.config.js —
# leave this unset locally. Set it for a production build where the
# frontend and backend are deployed to different origins.
# VITE_API_URL=https://your-backend.example.com/api
